{/* Radius.mdx */}
import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Fundação/Raio de Borda" />

# Raio de Borda

Base: **10px** — cantos generosos, herdados diretamente da identidade visual de referência do Prumo (o chip de link e os cards da peça original já usavam esse raio). Escala proporcional derivada de uma única variável `--radius` (era 6px antes da reforma de identidade).

---

<div style={{ display: 'flex', gap: 20, flexWrap: 'wrap', marginTop: 32, marginBottom: 32, alignItems: 'flex-end' }}>
  {[
    { name: 'rounded-none', px: '0px', radius: '0px' },
    { name: 'rounded-xs', px: '5px', radius: '5px' },
    { name: 'rounded-sm', px: '7px', radius: '7px' },
    { name: 'rounded-md', px: '10px', radius: '10px' },
    { name: 'rounded-lg', px: '13px', radius: '13px' },
    { name: 'rounded-xl', px: '20px', radius: '20px' },
    { name: 'rounded-2xl', px: '27px', radius: '27px' },
    { name: 'rounded-3xl', px: '40px', radius: '40px' },
    { name: 'rounded-full', px: '9999px', radius: '9999px' },
  ].map(r => (
    <div key={r.name} style={{ textAlign: 'center' }}>
      <div style={{
        width: 64,
        height: 64,
        background: 'oklch(0.527 0.145 36.8 / 15%)',
        border: '2px solid oklch(0.527 0.145 36.8)',
        borderRadius: r.radius,
        margin: '0 auto 10px',
      }} />
      <div style={{ fontSize: 11, fontWeight: 600, fontFamily: 'monospace' }}>{r.name}</div>
      <div style={{ fontSize: 10, color: '#94a3b8' }}>{r.px}</div>
    </div>
  ))}
</div>

---

## Uso recomendado por componente

| Componente | Raio |
|---|---|
| Inputs, selects, botões | `rounded-lg` (8px) |
| Cards | `rounded-xl` (12px) |
| Dialogs, modais | `rounded-2xl` (16px) |
| Badges, chips | `rounded-full` (9999px) |
| Tooltips | `rounded-xs` (3px) |
| Kbd | `rounded-xs` (3px) |
| Avatars | `rounded-full` |
| Toasts (Sonner) | `rounded-xl` |
